import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  loadingContainer: {
    width: 150,
    height: 65,
    backgroundColor: 'white',
    borderRadius: 5,
    shadowColor: '#000',
    shadowOpacity: 0.60,
    shadowRadius: 20,
    elevation: 10,
  },
});
